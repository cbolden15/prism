# Prism environment variables
#
# The deterministic demo and test suite require no credentials.
# Live provider examples read credentials through their provider CLI or
# process environment. Never commit provider tokens or credential files.
