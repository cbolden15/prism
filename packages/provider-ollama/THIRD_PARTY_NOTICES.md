# Third-party notices

`@useprism/provider-ollama` depends on `@useprism/sdk`, which is part of the
same Apache-2.0 Prism project. It has no external third-party runtime
dependencies. Repository build and test dependencies are recorded in
`package-lock.json`, summarized in the root `THIRD_PARTY_NOTICES.md`, and
validated in the full release license report.
